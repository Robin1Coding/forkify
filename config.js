// here will be all variables that should be constanst and should be reused across the project. It will allows us to simply add some changes
// here we want only important variables that are important for application itself

export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/'
export const TIMEOUT_SEC = 10